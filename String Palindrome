#include<string.h>
bool checkPalindrome(char str[]){

    int i,flag=0;
    int length=strlen(str);
    for(i=0;i<length;i++){
        if(str[i]!=str[length-i-1]){
            flag=1;
    }
  }
    if(flag==1)
        return 0;
    else
        return 1;
}
