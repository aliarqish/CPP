void SortZeroesAndOne(int arr[], int n){
      int i,count=0;
    for(i=0;i<n;i++){
        if(arr[i]==0)
            count++;
        }
    for(i=0;i<count;i++){
        arr[i]=0;
    }
    for(i=count;i<n;i++){
        arr[i]=1;
        
    }
}

