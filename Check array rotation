int FindSortedArrayRotation(int arr[], int n) {
    
	int i,k,flag=0;
    for(i=0;i<n;i++){
        if(arr[i]>arr[i+1]){
            flag=1;
            k=i+1;
          break;
        }

    }
    if(flag==1)
        return k;
    else
        return 0;
	
}
